/**
 * Axis spokes are drawn by `RadarGrid` (shared grid + spokes).
 * This placeholder keeps the suggested file structure for future
 * tick marks, value rings, or axis-specific decorations.
 */
export function RadarAxes() {
  return null
}
